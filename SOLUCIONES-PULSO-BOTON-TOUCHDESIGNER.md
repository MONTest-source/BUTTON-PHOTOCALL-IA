# Deprecado: flujo con botón

Este documento queda obsoleto. El flujo actual ya **no** usa `button1.click()`; ahora se controla el guardado directamente con `moviefileout1.par.record` y un `CHOP Execute DAT`.

Consulta la guía vigente en `TOUCHDESIGNER-BUTTON-INTEGRATION.md` (sección “TouchDesigner + ComfyTD — Auto-save cuando termina”).
