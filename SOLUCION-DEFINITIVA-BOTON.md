# Deprecado: flujo con botón

Este documento queda obsoleto. El flujo vigente ya **no** usa `button1.click()`; ahora el guardado se dispara vía `moviefileout1.par.record` desde un `CHOP Execute DAT` al detectar `uiprogress`.

Consulta la guía actualizada en `TOUCHDESIGNER-BUTTON-INTEGRATION.md` (“TouchDesigner + ComfyTD — Auto-save cuando termina”).
